{
  "name": "hanbee-attendance-dashboard-api",
  "version": "1.0.0",
  "private": true,
  "description": "Read-only Node/Express API that serves computed attendance summaries to the HR dashboard, reading from the same Aiven MySQL table the ESP32/Python backend writes to.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mysql2": "^3.11.0"
  }
}
